# JS, 1st, Squared Numbers Practice
c=[input()for _ in range(20)];print('\n'.join(f'{i},{int(i)*int(i)}'for i in c)) #80 characters
#c is set to the user's 20 numbers
#it then prints the number, ", ", and the squared of that number